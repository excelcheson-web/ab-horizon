# Cloudflare Pages – SPA routing
# Specific HTML entry points are served directly from dist (no redirect needed)
# The catch-all below handles client-side routing for the React SPA

/*    /index.html   200
